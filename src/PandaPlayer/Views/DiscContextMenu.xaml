<ContextMenu x:Class="PandaPlayer.Views.DiscContextMenu"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:interfaces="clr-namespace:PandaPlayer.ViewModels.Interfaces"
             mc:Ignorable="d" 
             Style="{DynamicResource MaterialDesignContextMenu}"
             d:DesignHeight="450" d:DesignWidth="800"
             d:DataContext="{d:DesignInstance interfaces:ILibraryExplorerViewModel}">

    <MenuItem Header="Play Disc" Command="{Binding PlayDiscCommand}" />
    <MenuItem Header="Add To Playlist" Command="{Binding AddDiscToPlaylistCommand}" />
    <MenuItem Header="Delete Disc" Command="{Binding DeleteDiscCommand}" />
    <MenuItem Header="Properties" Command="{Binding EditDiscPropertiesCommand}" />

</ContextMenu>
