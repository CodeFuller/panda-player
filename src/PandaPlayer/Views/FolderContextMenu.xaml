<ContextMenu x:Class="PandaPlayer.Views.FolderContextMenu"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:interfaces="clr-namespace:PandaPlayer.ViewModels.Interfaces"
             xmlns:behaviors="clr-namespace:PandaPlayer.Views.Behaviors"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             mc:Ignorable="d" 
             Style="{DynamicResource MaterialDesignContextMenu}"
             d:DesignHeight="450" d:DesignWidth="800"
             d:DataContext="{d:DesignInstance interfaces:ILibraryExplorerViewModel}">

    <MenuItem Header="Set Advise Group" behaviors:MenuBehavior.MenuItems="{Binding Path=AdviseGroupMenuItems, Mode=OneWay}">
        <MenuItem.Icon>
            <materialDesign:PackIcon Kind="FolderStar" />
        </MenuItem.Icon>
    </MenuItem>
    <MenuItem Header="Delete Folder" Command="{Binding DeleteFolderCommand}">
        <MenuItem.Icon>
            <materialDesign:PackIcon Kind="DeleteForever" />
        </MenuItem.Icon>
    </MenuItem>

</ContextMenu>
