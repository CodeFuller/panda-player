<UserControl x:Class="MusicLibrary.DiscPreprocessor.Views.AddToLibraryView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:i="http://schemas.microsoft.com/xaml/behaviors"
             mc:Ignorable="d" 
             d:DesignWidth="800" d:DesignHeight="400">

    <i:Interaction.Triggers>
        <i:EventTrigger EventName="Loaded">
            <i:InvokeCommandAction Command="{Binding AddToLibraryCommand}" />
        </i:EventTrigger>
    </i:Interaction.Triggers>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="35"/>
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <ProgressBar Grid.Row="0" Minimum="0" Maximum="{Binding ProgressSize}" Margin="0,0,0,5"
                     Value="{Binding CurrProgress, UpdateSourceTrigger=PropertyChanged}"/>
        <TextBox Grid.Row="1" x:Name="TextBoxProgressMessages" TextWrapping="NoWrap" IsReadOnly="True"
                 Text="{Binding ProgressMessages}" TextChanged="TextBoxProgressMessages_OnTextChanged" ScrollViewer.VerticalScrollBarVisibility="Visible"/>
    </Grid>
</UserControl>
