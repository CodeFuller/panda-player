<UserControl x:Class="CF.MusicLibrary.PandaPlayer.Views.DiscImageView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:valueConverters="clr-namespace:CF.MusicLibrary.PandaPlayer.Views.ValueConverters"
             mc:Ignorable="d"
             d:DesignWidth="300" d:DesignHeight="300" Background="#1B1B1B"> <!-- Can't make double-click event happen without setting Background property explicitly -->

    <UserControl.Resources>
        <valueConverters:ImageSourceValueConverter x:Key="ImageSourceValueConverter" />
    </UserControl.Resources>
    
    <Grid>
        <Image Source="{Binding CurrImageFileName, Converter={StaticResource ImageSourceValueConverter}}" />
    </Grid>
</UserControl>
