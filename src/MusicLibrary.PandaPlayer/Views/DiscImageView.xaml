<UserControl x:Class="MusicLibrary.PandaPlayer.Views.DiscImageView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:valueConverters="clr-namespace:MusicLibrary.PandaPlayer.Views.ValueConverters"
             mc:Ignorable="d"
             d:DesignWidth="400" d:DesignHeight="400">

    <UserControl.Resources>
        <valueConverters:ImageSourceValueConverter x:Key="ImageSourceValueConverter" />
    </UserControl.Resources>
    
    <Grid>
        <!-- Setting transparent background, because without background property double click event is not sent -->
        <Border Background="#00FFFFFF" >
            <Border.InputBindings>
                <MouseBinding MouseAction="LeftDoubleClick" Command="{Binding EditDiscImageCommand}"/>
            </Border.InputBindings>
            <Image Source="{Binding CurrentImageUri, Converter={StaticResource ImageSourceValueConverter}}" />
        </Border>
    </Grid>
</UserControl>
